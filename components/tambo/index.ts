/**
 * Tambo Components
 * 
 * Following CLAUDE.md template structure:
 * - Components are registered in src/lib/tambo.ts
 * - These are the actual component implementations
 */

export { TamboErrorBoundary } from './TamboErrorBoundary';
